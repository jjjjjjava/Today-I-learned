---
phase: 07-yaml-schema-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 00. learning/learning-behavior-system/interval-rules.md
  - 00. learning/learning-behavior-system/sub-points-rules.md
autonomous: true
requirements: [GRADUATE-01, GRADUATE-02, GRADUATE-03, SUBPOINT-01, SUBPOINT-02, SUBPOINT-03]

must_haves:
  truths:
    - "interval-rules.md 包含完整的 consecutive_success → 间隔天数查表，从 0 到 9+"
    - "interval-rules.md 包含 ok 结果的间隔上限规则（min(查表值, 上次间隔 x 1.3)）"
    - "interval-rules.md 包含毕业条件（consecutive_success >= 8 AND 间隔 >= 30 天）和毕业后行为"
    - "interval-rules.md 包含退步条件（consecutive_failures >= 3）和退步后行为"
    - "sub-points-rules.md 包含 sub_points 字段的完整 YAML schema 示例"
    - "sub-points-rules.md 包含 review_sessions 字段的完整 YAML schema 示例"
    - "sub-points-rules.md 包含 sub_point 状态更新规则（covered/uncovered 场景）"
  artifacts:
    - path: "00. learning/learning-behavior-system/interval-rules.md"
      provides: "动态间隔查表、毕业规则、退步规则的完整定义文档"
      contains: "consecutive_success"
    - path: "00. learning/learning-behavior-system/sub-points-rules.md"
      provides: "sub_points 字段规范、review_sessions 字段规范、状态更新规则"
      contains: "sub_points"
  key_links:
    - from: "interval-rules.md"
      to: "SKILL.md"
      via: "interval-rules 定义的字段与 SKILL.md YAML 字段定义一致"
      pattern: "consecutive_success"
    - from: "sub-points-rules.md"
      to: "SKILL.md"
      via: "sub-points-rules 定义的字段将在 Plan 02/03 中写入 SKILL.md"
      pattern: "sub_points"
---

<objective>
Create two rule definition documents that codify the dynamic interval/graduation/regression mechanism and the sub-points tracking mechanism for the learning behavior system.

Purpose: These documents are the authoritative reference for how Claude (the AI reviewer) calculates next_review intervals, determines graduation/regression, and manages sub-point status across review sessions. All subsequent YAML card modifications and SKILL.md updates depend on these rules being precisely defined first.

Output: Two Markdown rule documents in learning-behavior-system/ root directory.
</objective>

<execution_context>
@d:/Develop/Today-I-learned/项目/.planning/phases/07-yaml-schema-extension/07-RESEARCH.md
</execution_context>

<context>
@d:/Develop/Today-I-learned/项目/.planning/milestones/v1.1-学习系统基建改造/ROADMAP.md
@d:/Develop/Today-I-learned/项目/.planning/milestones/v1.1-学习系统基建改造/REQUIREMENTS.md
@d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interval-rules.md — dynamic interval lookup table, graduation, and regression rules</name>
  <files>d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md</files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/.planning/phases/07-yaml-schema-extension/07-RESEARCH.md (sections 2.1-2.4)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md (section "复习周期规则" and "YAML 字段定义")
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/009-MediaCodec编解码原理.yaml (example of card with consecutive_success=6)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/027-深入理解EGL.yaml (example of card with consecutive_success=1)
  </read_first>
  <action>
Create file `00. learning/learning-behavior-system/interval-rules.md` with these exact sections and content:

**Title:** `# 动态间隔与毕业/退步规则`

**Section 1: `## 动态间隔查表`**

Write a Markdown table with two columns: `consecutive_success` and `建议间隔（天）`. Use these exact values:

| consecutive_success | 建议间隔（天） |
|---------------------|---------------|
| 0 | 1 |
| 1 | 2 |
| 2 | 4 |
| 3 | 7 |
| 4 | 14 |
| 5 | 21 |
| 6 | 30 |
| 7 | 45 |
| 8 | 60 |
| 9+ | 90 |

Below the table, add a note: "增长率约 1.4-1.7x，比 SM-2 的 2.5x EF 保守，适合手动复习系统。"

**Section 2: `## 按 result 分类的间隔计算规则`**

Three subsections:

### result: good
- `consecutive_success += 1`
- `consecutive_failures = 0`（good 重置失败计数）
- `suggested_interval_days` = 查表值（按新的 consecutive_success 查上面的表）

### result: ok
- `consecutive_success += 1`（ok 等同 success，已有 memory 规则）
- `consecutive_failures = 0`
- `suggested_interval_days` = `min(查表值, 上次间隔 × 1.3)`
- 解释：ok 表示通过但有瑕疵，间隔增长受限，防止 ok 连击过快拉长间隔

### result: fail
- `consecutive_failures += 1`
- `consecutive_success` 不变（独立累计，已有 memory 规则）
- `suggested_interval_days = 1`（强制明天复习）

**Section 3: `## 毕业规则（GRADUATE-02）`**

Write the exact graduation condition:
```
毕业条件：consecutive_success >= 8 AND 上次实际间隔 >= 30 天
```

毕业后行为：
- `status` 从 `active` 改为 `archived`
- 文件保留在 `states/active/` 目录不移动
- 不再出现在每日复习队列
- 如需重新激活：手动将 `status` 改回 `active`，重置 `consecutive_success` 为 0

**Section 4: `## 退步规则（GRADUATE-03）`**

Write the exact regression condition:
```
退步条件：consecutive_failures >= 3（连续 3 次失败）
```

退步后行为：
- `next_review` = 明天（强制 1 天间隔）
- `consecutive_success` 不重置（保留历史累计）
- `consecutive_failures` 继续累计（不重置）
- 在 `last_assessment.weak_points` 中标注退步触发

Add a note explaining why threshold is 3 not 8: "本系统使用连续失败计数（consecutive），比 Anki 的累计失败计数（cumulative）信号更强，因此阈值设为 3 而非 Anki 默认的 8。"

**Section 5: `## 示例`**

Provide two concrete examples using real card data:

Example 1 — Card 009 (consecutive_success=6, result=good):
```
当前 consecutive_success: 6
本次 result: good
→ consecutive_success: 7
→ 查表: 7 → 45 天
→ suggested_interval_days: 45
→ next_review: today + 45 天
```

Example 2 — Card 027 (consecutive_success=1, result=ok, 上次间隔=2天):
```
当前 consecutive_success: 1
本次 result: ok
→ consecutive_success: 2
→ 查表: 2 → 4 天
→ ok 上限: min(4, 2 × 1.3) = min(4, 2.6) = 2.6 → 取整 3 天
→ suggested_interval_days: 3
→ next_review: today + 3 天
```
  </action>
  <verify>
    <automated>grep -c "动态间隔查表" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md" &amp;&amp; grep -c "consecutive_success >= 8" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md" &amp;&amp; grep -c "consecutive_failures >= 3" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md" &amp;&amp; grep -c "上次间隔 × 1.3" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md"</automated>
  </verify>
  <acceptance_criteria>
    - interval-rules.md contains "## 动态间隔查表" with a 10-row table (0 through 9+)
    - interval-rules.md contains exact text "consecutive_success >= 8 AND 上次实际间隔 >= 30 天"
    - interval-rules.md contains exact text "consecutive_failures >= 3"
    - interval-rules.md contains "min(查表值, 上次间隔 × 1.3)" for ok result
    - interval-rules.md contains "result: good", "result: ok", "result: fail" subsections
    - interval-rules.md contains "status" 从 "active" 改为 "archived"
    - interval-rules.md contains two worked examples using card 009 and card 027
  </acceptance_criteria>
  <done>interval-rules.md exists with complete dynamic interval lookup table, graduation rule (consecutive_success >= 8 AND interval >= 30), regression rule (consecutive_failures >= 3), ok-cap formula, and two worked examples</done>
</task>

<task type="auto">
  <name>Task 2: Create sub-points-rules.md — sub_points schema, review_sessions schema, and status update rules</name>
  <files>d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md</files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/.planning/phases/07-yaml-schema-extension/07-RESEARCH.md (sections 3.1-3.4)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md (section "YAML 字段定义")
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/017-OpenGL-ES零拷贝图形管线.yaml (will become example for sub_points)
  </read_first>
  <action>
Create file `00. learning/learning-behavior-system/sub-points-rules.md` with these exact sections:

**Title:** `# Sub-points 子知识点追踪规则`

**Section 1: `## 设计原则`**
- sub_points 是卡片内的静态知识点列表，不独立调度（卡片仍是最小调度单位）
- 每张卡片最多 8 个 sub_points
- 没有 sub_points 字段的卡片按现有规则处理，向后兼容
- sub_points 的状态跨复习会话累计追踪，不自动重置

**Section 2: `## sub_points 字段 Schema`**

Provide the exact YAML schema with inline comments:

```yaml
sub_points:
  - id: sp1                          # 短 ID，在 review_sessions 中引用
    desc: "知识点描述"                 # 人类可读描述
    status: weak                     # weak | ok | good（跨会话累计）
    last_reviewed: 2026-03-27        # 上次复习此 sub_point 的日期
```

Field rules:
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 格式 sp1, sp2, ... sp8，卡片内唯一 |
| desc | string | 子知识点的一句话描述 |
| status | enum | weak / ok / good，初始值为 weak |
| last_reviewed | date | 格式 YYYY-MM-DD，该 sub_point 上次被覆盖的日期 |

**Section 3: `## review_sessions 字段 Schema`**

Provide the exact YAML schema:

```yaml
review_sessions:
  - date: 2026-03-27
    covered_sub_points: [sp1, sp2]   # 本次覆盖的 sub_point id 列表
    result: ok                       # 本次整体结果
    score: 3                         # 本次整体评分
```

Field rules:
| 字段 | 类型 | 说明 |
|------|------|------|
| date | date | 复习日期 |
| covered_sub_points | list | 本次覆盖的 sub_point id 列表 |
| result | enum | fail / ok / good（本次整体结果） |
| score | int | 1-5 评分 |

规则：
- review_sessions 是 append-only 日志，只追加不修改历史记录
- 每次复习生成一条记录
- covered_sub_points 列出本次实际考察到的 sub_point id

**Section 4: `## sub_point 状态更新规则`**

Write the exact update logic:

```
每次复习后，对每个 sub_point 执行以下判断：

情况 A — 本次 covered（出现在 covered_sub_points 列表中）：
  整体 result = good → sub_point.status 升至 good
  整体 result = ok   → sub_point.status 至少升至 ok（如果原来是 weak 则升为 ok，如果已经是 good 则保持 good）
  整体 result = fail → sub_point.status 降至 weak
  更新 sub_point.last_reviewed 为本次日期

情况 B — 本次未 covered（不在 covered_sub_points 列表中）：
  sub_point.status 不变（保留上次状态）
  sub_point.last_reviewed 不变
  → 这是核心设计：未覆盖不等于遗忘，状态保持
```

**Section 5: `## 向后兼容`**
- 没有 sub_points 字段的卡片：按现有规则处理，不报错
- 没有 review_sessions 字段的卡片：正常复习，只是没有会话级追踪
- 迁移方式：逐步为卡片添加 sub_points，不需要一次性全部迁移

**Section 6: `## 完整示例 — Card 017 OpenGL ES 零拷贝图形管线`**

Based on card 017's notes, provide a complete example showing both sub_points and review_sessions:

```yaml
sub_points:
  - id: sp1
    desc: "BufferQueue 三缓冲机制（producer/consumer + 3 个 GraphicBuffer）"
    status: good
    last_reviewed: 2026-03-27
  - id: sp2
    desc: "updateTexImage 本质（引用更新非拷贝，在 GL 线程调用）"
    status: ok
    last_reviewed: 2026-03-27
  - id: sp3
    desc: "EGLImage 绑定机制（eglCreateImageKHR → glEGLImageTargetTexture2DOES）"
    status: weak
    last_reviewed: 2026-03-20
  - id: sp4
    desc: "GL_TEXTURE_EXTERNAL_OES vs GL_TEXTURE_2D（外部纹理 = 引用不拥有）"
    status: ok
    last_reviewed: 2026-03-27
  - id: sp5
    desc: "完整数据流：MediaCodec → GraphicBuffer → BufferQueue → SurfaceTexture → GPU"
    status: good
    last_reviewed: 2026-03-27

review_sessions:
  - date: 2026-03-27
    covered_sub_points: [sp1, sp2, sp4, sp5]
    result: ok
    score: 3
```

Then walk through the status update: "sp1 covered + result ok → status stays good (already good). sp2 covered + result ok → status stays ok. sp3 NOT covered → status stays weak. sp4 covered + result ok → status stays ok. sp5 covered + result ok → status stays good."
  </action>
  <verify>
    <automated>grep -c "sub_points" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md" &amp;&amp; grep -c "review_sessions" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md" &amp;&amp; grep -c "weak | ok | good" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md" &amp;&amp; grep -c "covered_sub_points" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md"</automated>
  </verify>
  <acceptance_criteria>
    - sub-points-rules.md contains "## sub_points 字段 Schema" with YAML code block
    - sub-points-rules.md contains "## review_sessions 字段 Schema" with YAML code block
    - sub-points-rules.md contains "## sub_point 状态更新规则" with covered and uncovered logic
    - sub-points-rules.md contains "weak | ok | good" as the status enum
    - sub-points-rules.md contains "covered_sub_points" in the review_sessions schema
    - sub-points-rules.md contains "append-only" describing review_sessions behavior
    - sub-points-rules.md contains complete Card 017 example with 5 sub_points
    - sub-points-rules.md contains "最多 8 个 sub_points"
    - sub-points-rules.md contains "向后兼容" section
  </acceptance_criteria>
  <done>sub-points-rules.md exists with complete sub_points schema, review_sessions schema, status update rules (covered/uncovered), backward compatibility notes, and a worked Card 017 example</done>
</task>

</tasks>

<verification>
1. Both rule documents exist in learning-behavior-system/ root
2. interval-rules.md covers GRADUATE-01 (lookup table), GRADUATE-02 (graduation), GRADUATE-03 (regression)
3. sub-points-rules.md covers SUBPOINT-01 (schema), SUBPOINT-02 (review_sessions), SUBPOINT-03 (status tracking)
4. All concrete values match 07-RESEARCH.md recommendations
5. Examples use real card data (009, 017, 027)
</verification>

<success_criteria>
- interval-rules.md is self-contained: any reader can compute next_review for any card without consulting RESEARCH.md
- sub-points-rules.md is self-contained: any reader can add sub_points to a card and update status after a review session
- All 6 requirement IDs (GRADUATE-01/02/03, SUBPOINT-01/02/03) have their rules fully specified
</success_criteria>

<output>
After completion, create `.planning/phases/07-yaml-schema-extension/07-01-SUMMARY.md`
</output>

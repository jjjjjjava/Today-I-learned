---
phase: 07-yaml-schema-extension
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - 00. learning/learning-behavior-system/states/active/020-OpenGL-ES纹理系统与绘制模式.yaml
  - 00. learning/learning-behavior-system/states/active/021-双Surface视频渲染架构与FBO.yaml
  - 00. learning/learning-behavior-system/states/active/022-正交投影矩阵与视频比例适配.yaml
  - 00. learning/learning-behavior-system/states/active/023-OpenGL坐标系转换与矩阵GPU传递.yaml
  - 00. learning/learning-behavior-system/states/active/024-OpenGL视频渲染完整实战.yaml
  - 00. learning/learning-behavior-system/states/active/025-OpenGL多画面渲染与Alpha混合.yaml
  - 00. learning/learning-behavior-system/states/active/026-OpenGL矩阵平移与缩放交互.yaml
  - 00. learning/learning-behavior-system/states/active/027-深入理解EGL.yaml
autonomous: true
requirements: [SUBPOINT-01, SUBPOINT-02, SUBPOINT-03]

must_haves:
  truths:
    - "Cards 020-027 each have a sub_points field with 3-8 items"
    - "Cards 020-027 each have a review_sessions field with at least one historical entry"
    - "Sub_points content accurately reflects each card's notes and weak_points"
  artifacts:
    - path: "00. learning/learning-behavior-system/states/active/020-OpenGL-ES纹理系统与绘制模式.yaml"
      provides: "Card with sub_points and review_sessions added"
      contains: "sub_points:"
    - path: "00. learning/learning-behavior-system/states/active/027-深入理解EGL.yaml"
      provides: "Card with sub_points and review_sessions added"
      contains: "sub_points:"
  key_links:
    - from: "每张 YAML 卡片的 sub_points"
      to: "sub-points-rules.md"
      via: "sub_points schema 遵循 rules 文档定义"
      pattern: "sub_points:"
---

<objective>
Add sub_points and review_sessions fields to the second batch of 8 active YAML cards (020-027). This plan runs in parallel with Plan 02 since they modify different files.

Purpose: Complete the migration of all active cards to the new schema, ensuring every card in the system can track sub-knowledge-point mastery.

Output: 8 updated YAML cards with sub_points and review_sessions.
</objective>

<execution_context>
@d:/Develop/Today-I-learned/项目/.planning/phases/07-yaml-schema-extension/07-RESEARCH.md
</execution_context>

<context>
@d:/Develop/Today-I-learned/项目/.planning/milestones/v1.1-学习系统基建改造/ROADMAP.md
@d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (created by Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub_points and review_sessions to cards 020, 021, 022, 023</name>
  <files>
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/020-OpenGL-ES纹理系统与绘制模式.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/021-双Surface视频渲染架构与FBO.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/022-正交投影矩阵与视频比例适配.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/023-OpenGL坐标系转换与矩阵GPU传递.yaml
  </files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (Plan 01 output: schema and rules)
    - Each of the 4 YAML card files listed above (read notes and weak_points to extract sub_points)
  </read_first>
  <action>
For each of the 4 cards, append `sub_points:` and `review_sessions:` at the end of the file.

**Process for each card:**
1. Read the card's `notes:` field to identify distinct knowledge concepts (3-8 per card)
2. Read `last_assessment.weak_points` to identify which concepts are weak/ok
3. Create sub_points entries following sub-points-rules.md schema:
   - id: sp1, sp2, ... spN
   - desc: one-line description of the knowledge concept
   - status: weak (if in weak_points), ok (if partially understood), good (if solid)
   - last_reviewed: use the card's last_study date
4. Create one review_sessions entry from last_assessment data

**Specific guidance:**

Card 020 (OpenGL ES 纹理系统与绘制模式):
Extract sub_points about: texture loading, texture coordinates, GL_TRIANGLE_STRIP vs GL_TRIANGLE_FAN, texture filtering (GL_LINEAR/GL_NEAREST), mipmap, etc. Read notes for exact content.

Card 021 (双Surface视频渲染架构与FBO):
Extract sub_points about: SurfaceView vs TextureView, FBO concept (pointer set not storage), default FBO vs custom FBO, double-buffer rendering, etc. Read notes for exact content.

Card 022 (正交投影矩阵与视频比例适配):
Extract sub_points about: orthographic projection matrix, aspect ratio calculation, viewport vs projection, Matrix.orthoM parameters, etc. Read notes for exact content.

Card 023 (OpenGL坐标系转换与矩阵GPU传递):
Extract sub_points about: coordinate system differences (OpenGL vs screen vs texture), matrix transformation order, glUniformMatrix4fv, MVP matrix, etc. Read notes for exact content.

**IMPORTANT:**
- Do NOT modify any existing YAML fields
- Only APPEND sub_points and review_sessions at the end
- Follow exact schema from sub-points-rules.md
- Each sub_point status: weak | ok | good
  </action>
  <verify>
    <automated>for f in 020 021 022 023; do echo "--- Card $f ---"; grep -c "sub_points:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; grep -c "review_sessions:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; done</automated>
  </verify>
  <acceptance_criteria>
    - Cards 020, 021, 022, 023 each contain "sub_points:" field
    - Cards 020, 021, 022, 023 each contain "review_sessions:" field with at least one entry
    - Each sub_points entry has id, desc, status, last_reviewed
    - Each card has between 3 and 8 sub_points
    - Sub_point descriptions are derived from each card's actual notes content
    - No existing YAML fields modified
  </acceptance_criteria>
  <done>Cards 020-023 have sub_points and review_sessions fields following sub-points-rules.md schema</done>
</task>

<task type="auto">
  <name>Task 2: Add sub_points and review_sessions to cards 024, 025, 026, 027</name>
  <files>
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/024-OpenGL视频渲染完整实战.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/025-OpenGL多画面渲染与Alpha混合.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/026-OpenGL矩阵平移与缩放交互.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/027-深入理解EGL.yaml
  </files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (Plan 01 output)
    - Each of the 4 YAML card files listed above
  </read_first>
  <action>
For each of the 4 cards, append `sub_points:` and `review_sessions:` at the end of the file.

Same process as Task 1. Read notes, extract sub_points, create review_sessions entry.

**Specific guidance:**

Card 024 (OpenGL视频渲染完整实战):
Extract sub_points about the complete rendering pipeline in practice. Read notes for exact content.

Card 025 (OpenGL多画面渲染与Alpha混合):
Extract sub_points about multi-surface rendering and alpha blending. Read notes for exact content.

Card 026 (OpenGL矩阵平移与缩放交互):
Extract sub_points about matrix translation, scaling, and user interaction. Read notes for exact content.

Card 027 (深入理解EGL, consecutive_success=1):
Sub_points from the detailed notes already available:
- sp1: "EGL 两大职责（桥接 OpenGL 与窗口系统 + 管理 Context）"
- sp2: "FBO 结构（指针集合：默认 FBO(0) vs 自定义 FBO）"
- sp3: "eglMakeCurrent 机制（TLS 存储 + 默认 FBO color attachment 绑定）"
- sp4: "EGLContext = 线程隔离工具箱（状态层 + 资源池 + 共享 Context）"
- sp5: "五层代码架构（Activity → Renderer → RenderThread → EGLSurfaceHolder → EGLCore）"
- sp6: "VSync 完整通信机制（路线A: socket+epoll → UI，路线B: 信号量+futex → GL线程）"
- sp7: "SurfaceView 本质（打洞 + 独立 Surface）vs TextureView（纹理贴回 View 树）"
- sp8: "eglCreateWindowSurface 本质（封装 Surface + 连接 BufferQueue + dequeue 第一块 buffer）"
Status: sp2 and sp6 are ok (partially covered in weak_points area), sp3 could be ok (createOffscreenSurface noted as uncovered). Others good based on detailed notes coverage.

**IMPORTANT:**
- Do NOT modify any existing YAML fields
- Only APPEND sub_points and review_sessions at the end
- Card 027 should have exactly 8 sub_points as specified above (maximum allowed)
  </action>
  <verify>
    <automated>for f in 024 025 026 027; do echo "--- Card $f ---"; grep -c "sub_points:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; grep -c "review_sessions:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; done</automated>
  </verify>
  <acceptance_criteria>
    - Cards 024, 025, 026, 027 each contain "sub_points:" field
    - Cards 024, 025, 026, 027 each contain "review_sessions:" field with at least one entry
    - Card 027 has exactly 8 sub_points covering EGL core concepts
    - Each sub_points entry has id, desc, status, last_reviewed
    - Each card has between 3 and 8 sub_points
    - No existing YAML fields modified
  </acceptance_criteria>
  <done>Cards 024-027 have sub_points and review_sessions fields. All 15 active cards now have the new schema fields.</done>
</task>

</tasks>

<verification>
1. All 8 cards (020-027) have sub_points and review_sessions fields
2. Card 027 has 8 sub_points (maximum) covering its extensive notes
3. All sub_points follow the schema: id, desc, status, last_reviewed
4. All review_sessions follow the schema: date, covered_sub_points, result, score
5. Spot-check: card 027 still has consecutive_success: 1 (no existing field modified)
</verification>

<success_criteria>
- Combined with Plan 02, ALL 15 active cards now have sub_points and review_sessions
- Sub_points content is meaningful (derived from actual card notes, not generic placeholders)
- The learning behavior system can now track sub-knowledge-point mastery for every active card
</success_criteria>

<output>
After completion, create `.planning/phases/07-yaml-schema-extension/07-03-SUMMARY.md`
</output>

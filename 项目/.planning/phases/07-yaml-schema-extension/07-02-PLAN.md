---
phase: 07-yaml-schema-extension
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - 00. learning/learning-behavior-system/states/active/009-MediaCodec编解码原理.yaml
  - 00. learning/learning-behavior-system/states/active/010-音视频基础概念.yaml
  - 00. learning/learning-behavior-system/states/active/011-音视频同步原理.yaml
  - 00. learning/learning-behavior-system/states/active/014-音视频重封装MP4.yaml
  - 00. learning/learning-behavior-system/states/active/017-OpenGL-ES零拷贝图形管线.yaml
  - 00. learning/learning-behavior-system/states/active/018-OpenGL-ES渲染管线基础.yaml
  - 00. learning/learning-behavior-system/states/active/019-GLSL与Android渲染架构.yaml
  - 00. learning/learning-behavior-system/SKILL.md
autonomous: true
requirements: [SUBPOINT-01, SUBPOINT-02, SUBPOINT-03, GRADUATE-01, GRADUATE-02]

must_haves:
  truths:
    - "Cards 009, 010, 011, 014, 017, 018, 019 each have a sub_points field with 3-8 items"
    - "Cards 009, 010, 011, 014, 017, 018, 019 each have a review_sessions field with at least one historical entry"
    - "SKILL.md documents sub_points and review_sessions in its YAML field definition section"
    - "SKILL.md documents the archived status and dynamic interval rules"
  artifacts:
    - path: "00. learning/learning-behavior-system/states/active/009-MediaCodec编解码原理.yaml"
      provides: "Card with sub_points and review_sessions added"
      contains: "sub_points:"
    - path: "00. learning/learning-behavior-system/states/active/017-OpenGL-ES零拷贝图形管线.yaml"
      provides: "Card with sub_points and review_sessions added"
      contains: "sub_points:"
    - path: "00. learning/learning-behavior-system/SKILL.md"
      provides: "Updated system documentation with new fields"
      contains: "sub_points"
  key_links:
    - from: "每张 YAML 卡片的 sub_points"
      to: "sub-points-rules.md"
      via: "sub_points schema 遵循 rules 文档定义"
      pattern: "sub_points:"
    - from: "SKILL.md 新增字段定义"
      to: "interval-rules.md"
      via: "SKILL.md 引用 interval-rules.md 中的动态间隔规则"
      pattern: "interval-rules.md"
---

<objective>
Add sub_points and review_sessions fields to the first batch of 7 active YAML cards (009-019), and update SKILL.md to document all new fields introduced by Phase 7.

Purpose: Transform the first batch of cards to the new schema so the learning behavior system can track sub-knowledge-point mastery. Update SKILL.md so that Claude (the AI reviewer) knows the new fields exist and how to use them.

Output: 7 updated YAML cards + updated SKILL.md
</objective>

<execution_context>
@d:/Develop/Today-I-learned/项目/.planning/phases/07-yaml-schema-extension/07-RESEARCH.md
</execution_context>

<context>
@d:/Develop/Today-I-learned/项目/.planning/milestones/v1.1-学习系统基建改造/ROADMAP.md
@d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md (created by Plan 01)
@d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (created by Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub_points and review_sessions to cards 009, 010, 011, 014, 017, 018, 019</name>
  <files>
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/009-MediaCodec编解码原理.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/010-音视频基础概念.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/011-音视频同步原理.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/014-音视频重封装MP4.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/017-OpenGL-ES零拷贝图形管线.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/018-OpenGL-ES渲染管线基础.yaml
    d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/019-GLSL与Android渲染架构.yaml
  </files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (Plan 01 output: schema and rules)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md (Plan 01 output: interval rules)
    - Each of the 7 YAML card files listed above (read each card's notes and last_assessment.weak_points to extract sub_points)
  </read_first>
  <action>
For each of the 7 cards, append two new fields at the end of the YAML file (after `notes:`): `sub_points:` and `review_sessions:`.

**How to extract sub_points for each card:**
1. Read the card's `notes:` field — identify distinct knowledge concepts mentioned
2. Read the card's `last_assessment.weak_points` — these indicate areas that should be separate sub_points
3. Create 3-8 sub_points per card, each with: id (sp1..spN), desc (one-line description), status (derive from weak_points: if it appears in weak_points → weak or ok; if not mentioned as weak → good), last_reviewed (use last_study date from the card)

**How to create the initial review_sessions entry:**
Use the card's `last_assessment` data to create one historical session entry:
```yaml
review_sessions:
  - date: {last_assessment.date}
    covered_sub_points: [sp1, sp2, ...]  # Include all sub_points as covered for the initial entry
    result: {last_assessment.result}
    score: {last_assessment.score}
```

**Specific guidance per card:**

Card 009 (MediaCodec编解码原理, consecutive_success=6):
Sub_points to extract from notes ("硬编硬解、异步同步模式、常见问题"):
- sp1: "MediaCodec 硬编硬解流程（configure → start → dequeueInput → queueInput → dequeueOutput）"
- sp2: "同步模式 vs 异步模式（Callback）的使用场景与区别"
- sp3: "常见问题排查（绿屏、花屏的原因与解决）"
- sp4: "MediaExtractor 解封装 vs MediaMuxer 封装（读取 vs 写入，不要混淆）"
Status: sp4 is weak (from weak_points), others good (consecutive_success=6)

Card 010 (音视频基础概念):
Read card notes, extract 3-5 sub_points covering audio/video fundamentals.

Card 011 (音视频同步原理):
Read card notes, extract 3-5 sub_points covering A/V sync mechanisms.

Card 014 (音视频重封装MP4):
Read card notes, extract 3-5 sub_points covering remuxing concepts.

Card 017 (OpenGL ES 零拷贝图形管线, consecutive_success=5):
Sub_points from notes and research example:
- sp1: "BufferQueue 三缓冲机制（producer/consumer + 3 个 GraphicBuffer/ION 共享显存）"
- sp2: "updateTexImage 本质（引用更新非拷贝，必须在 GL 线程调用）"
- sp3: "EGLImage 绑定机制（eglCreateImageKHR → glEGLImageTargetTexture2DOES）"
- sp4: "GL_TEXTURE_EXTERNAL_OES vs GL_TEXTURE_2D（外部纹理 = 引用不拥有内存）"
- sp5: "完整数据流：MediaCodec → GraphicBuffer → BufferQueue → SurfaceTexture → GPU → 屏幕"
Status: sp2 is ok (weak_point mentions updateTexImage confusion), sp3 is weak (not recently reviewed well), others good

Card 018 (OpenGL ES 渲染管线基础):
Read card notes, extract 4-6 sub_points covering the rendering pipeline stages.

Card 019 (GLSL 与 Android 渲染架构):
Read card notes, extract 4-6 sub_points covering shader language and Android rendering.

**IMPORTANT:**
- Do NOT modify any existing YAML fields (id, title, status, consecutive_success, etc.)
- Only APPEND sub_points and review_sessions at the end of each file
- Follow the exact schema from sub-points-rules.md
- Each sub_point status must be one of: weak, ok, good
- Use the card's actual last_study date as last_reviewed for all sub_points
  </action>
  <verify>
    <automated>for f in 009 010 011 014 017 018 019; do echo "--- Card $f ---"; grep -c "sub_points:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; grep -c "review_sessions:" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/states/active/"*"$f"*.yaml; done</automated>
  </verify>
  <acceptance_criteria>
    - All 7 cards (009, 010, 011, 014, 017, 018, 019) contain "sub_points:" field
    - All 7 cards contain "review_sessions:" field with at least one entry
    - Each sub_points entry has id, desc, status, last_reviewed fields
    - Each review_sessions entry has date, covered_sub_points, result, score fields
    - Card 017 has exactly 5 sub_points matching the research example
    - Card 009 has sp4 with status: weak (MediaExtractor/MediaMuxer confusion)
    - No existing YAML fields were modified (id, title, status, consecutive_success all unchanged)
    - Each card has between 3 and 8 sub_points
  </acceptance_criteria>
  <done>All 7 cards have sub_points with 3-8 items each and review_sessions with initial entry, all following sub-points-rules.md schema exactly</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md with new field definitions and rule document references</name>
  <files>d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md</files>
  <read_first>
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md (full file)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/interval-rules.md (Plan 01 output)
    - d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/sub-points-rules.md (Plan 01 output)
  </read_first>
  <action>
Make these specific edits to SKILL.md:

**Edit 1: Update YAML field definition section (section "YAML 字段定义")**

In the "知识点类型" YAML example block, add these new fields after `suggested_interval_days`:

```yaml
sub_points:                          # 子知识点列表（可选，最多8个）
  - id: sp1                          # 短 ID（sp1-sp8）
    desc: "子知识点描述"               # 一句话描述
    status: weak                     # weak | ok | good（跨会话累计）
    last_reviewed: 2026-03-27        # 上次复习此子知识点的日期
review_sessions:                     # 复习会话日志（append-only）
  - date: 2026-03-27
    covered_sub_points: [sp1, sp2]   # 本次覆盖的子知识点
    result: ok                       # 本次整体结果
    score: 3                         # 1-5 评分
```

**Edit 2: Update status enum**

In the status field comment, change from:
```yaml
status: active                    # 状态：pool | active | review | mastered
```
To:
```yaml
status: active                    # 状态：pool | active | archived | review | mastered
```

Add a note below the YAML block:
"`archived` 状态：卡片达到毕业条件后自动设置。文件保留在 `states/active/` 目录，不再出现在每日复习队列。详见 interval-rules.md。"

**Edit 3: Update 复习周期规则 section**

Add a new subsection at the beginning of "复习周期规则":

```markdown
### 动态间隔规则（v1.1 新增）

从 v1.1 开始，`next_review` 主要由 `consecutive_success` 驱动的动态间隔查表决定，不再仅依赖 `review_stage` 基准值。

完整规则详见：[interval-rules.md](interval-rules.md)

核心变化：
- `result: good` → 查表取间隔，间隔按 consecutive_success 递增
- `result: ok` → 查表取间隔，但不超过上次间隔的 1.3 倍
- `result: fail` → 强制 1 天间隔
- 毕业条件：consecutive_success >= 8 AND 间隔 >= 30 天 → status 变为 archived
- 退步条件：consecutive_failures >= 3 → next_review 重置为明天

原有 `review_stage` 基准值仍保留作为参考，但以动态间隔查表为准。
```

**Edit 4: Add sub_points reference in 目录**

In the table of contents section, add after the last entry:
```markdown
- [子知识点追踪规则](#子知识点追踪规则)
```

And add a new section before "注意事项":

```markdown
## 子知识点追踪规则

每张卡片可选包含 `sub_points` 字段，追踪卡片内的独立子知识点掌握状态。

完整规则详见：[sub-points-rules.md](sub-points-rules.md)

核心要点：
- 每张卡片最多 8 个 sub_points，每个有独立的 weak/ok/good 状态
- 每次复习后在 `review_sessions` 中追加一条记录，记录本次覆盖了哪些 sub_points
- 未覆盖的 sub_point 状态保持不变（不自动重置）
- 没有 sub_points 字段的卡片按现有规则处理，向后兼容
```

**Edit 5: Update 目录结构 section**

In the directory structure, add the two new files:
```
learning-behavior-system/
├── SKILL.md
├── interval-rules.md         # 动态间隔/毕业/退步规则（v1.1 新增）
├── sub-points-rules.md       # 子知识点追踪规则（v1.1 新增）
├── states/
...
```

**IMPORTANT:** Preserve ALL existing content. Only ADD new sections and fields. Do not remove or rewrite existing sections.
  </action>
  <verify>
    <automated>grep -c "sub_points" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md" &amp;&amp; grep -c "review_sessions" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md" &amp;&amp; grep -c "interval-rules.md" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md" &amp;&amp; grep -c "sub-points-rules.md" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md" &amp;&amp; grep -c "archived" "d:/Develop/Today-I-learned/项目/00. learning/learning-behavior-system/SKILL.md"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "sub_points" in the YAML field definition example
    - SKILL.md contains "review_sessions" in the YAML field definition example
    - SKILL.md contains "interval-rules.md" as a link reference
    - SKILL.md contains "sub-points-rules.md" as a link reference
    - SKILL.md contains "archived" in the status enum
    - SKILL.md contains "动态间隔规则（v1.1 新增）" subsection
    - SKILL.md contains "子知识点追踪规则" section
    - SKILL.md directory structure includes interval-rules.md and sub-points-rules.md
    - All original content is preserved (no sections removed)
  </acceptance_criteria>
  <done>SKILL.md documents sub_points, review_sessions, archived status, and references both rule documents. All existing content preserved.</done>
</task>

</tasks>

<verification>
1. All 7 cards (009-019) have sub_points and review_sessions fields
2. SKILL.md references interval-rules.md and sub-points-rules.md
3. SKILL.md YAML field definition includes sub_points and review_sessions
4. SKILL.md status enum includes "archived"
5. No existing card data was modified (spot-check: card 009 still has consecutive_success: 6)
</verification>

<success_criteria>
- Every card in the first batch has 3-8 sub_points derived from its actual notes content
- Every card has at least one review_sessions entry based on its last_assessment
- SKILL.md is the single source of truth for all YAML fields, now including v1.1 additions
- A reader of SKILL.md can understand the full schema without consulting RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-yaml-schema-extension/07-02-SUMMARY.md`
</output>

# K05 · ABC控制变量

```text
A：旧AGP，插件正常
B：新AGP，插桩失效
C：新AGP，插桩修复
```

- `B→C`：构建环境相同，隔离ARouter插桩修复收益。
- `A→C`：插件都正常，观察AGP升级后的剩余差异。

设备档位、构建类型、启动入口、终点和数据状态必须一致。

检索题：

1. A/B/C各是什么？
2. `B→C`和`A→C`分别回答什么？
3. 哪些变量必须固定？

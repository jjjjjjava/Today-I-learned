# K09 · 插件注册与Dex扫描

插件路径在编译期收集生成类，并把`register()`写入`loadRouterMap()`；运行时直接注册。

默认路径在运行时打开Dex、枚举全部类名、过滤routes类，再反射`loadInto()`。

两条路径都需要装载路由表；区别是“生成类怎样被发现”。

检索题：

1. 两条路径分别在什么时候发现生成类？
2. 插件消除了什么成本？
3. 为什么不能说插件删除了`loadInto()`？

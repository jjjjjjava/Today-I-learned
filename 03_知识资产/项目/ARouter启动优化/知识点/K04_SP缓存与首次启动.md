# K04 · SP缓存与首次启动

ARouter运行时扫描后，会把生成类名集合写入SP。

```text
首次安装/升级首启：缓存无效，可能完整扫描Dex
后续启动：读取类名集合，跳过完整扫描
```

SP缓存的不是完整RouteMeta，也不是Gradle插件缓存。

若Benchmark丢弃首轮或只看多轮中位数，真实升级用户的首次劣化会被隐藏。

检索题：

1. SP缓存保存什么？
2. 哪些场景会重新扫描？
3. 为什么CI和中位数会漏掉问题？

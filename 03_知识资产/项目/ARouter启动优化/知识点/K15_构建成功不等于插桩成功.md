# K15 · 构建成功不等于插桩成功

插件编译通过，只说明构建没有立即失败。仍可能存在：

- 目标variant任务未执行；
- 驼峰variant命名未匹配；
- 路由表集合不完整；
- `loadRouterMap()`未写入`register()`；
- 运行时仍走Dex扫描。

必须同时验证构建日志、修改后产物、`registerByPlugin`、调用栈、功能与性能。

检索题：

1. 构建成功覆盖了哪些层？
2. variant命名问题怎样让优化静默失效？
3. 怎样证明插桩真正生效？

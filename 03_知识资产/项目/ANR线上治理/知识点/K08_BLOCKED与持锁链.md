# K08 · BLOCKED 与持锁链

`BLOCKED` 表示主线程等待进入 Java monitor。

```text
main 等锁 A
→ A 由线程 58 持有
→ 检查线程 58
→ 它可能执行耗时任务、等待锁 B，或形成循环依赖
```

“主线程等锁 A”是直接原因，不是最终根因。必须继续沿持锁关系追到能够解释长时间不释放的执行单元。

## 自检

1. 如何从 trace 找到持锁线程？
2. AB-BA 死锁怎样形成闭环？
3. 为什么不能把 `BLOCKED` 直接当根因？

## 相关

- [[../阶段/05_单点归因_主线程与堆栈状态]]

# 鸿蒙 FFmpeg · 独立知识点题库

1. fftools 是库还是编排层？
2. Lycium 是编译器吗？
3. 为什么不能把所有 `exit_program` 改成 `return`？
4. 为什么先保存返回码再 reset？
5. TLS 为什么不代表并发安全？
6. `.d.ts` 为什么不能创建运行时函数？
7. `PFUNCTION` 的 `P` 保证什么？
8. 为什么还要把 `vector<string>` 转回 `argc/argv`？
9. UUID、`g_callbacks`、`g_currentCallback` 如何分工？
10. codec 名正确却不存在时检查哪里？
11. `-b:v` 的参数链如何下钻？
12. 视频轨码率与总码率有什么区别？
13. `150s → 27s` 的两种提升口径怎样计算？

## 核对

- [[../知识点/知识点索引]]

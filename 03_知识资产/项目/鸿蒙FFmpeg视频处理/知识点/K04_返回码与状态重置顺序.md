# K04 · 返回码与状态重置顺序

```text
保存 main_return_code
→ reset_ffmpeg_state()
→ return saved_error
```

reset 会清理输入输出表、计数器、回调及返回状态。若先 reset，再读取返回码，上层可能把失败误判为成功。

## 自检

1. 为什么嵌入 App 后必须显式 reset？
2. 顺序颠倒会产生什么错误？
3. 哪类问题可能只在第二次命令暴露？

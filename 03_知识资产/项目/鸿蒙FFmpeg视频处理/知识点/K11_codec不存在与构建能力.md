# K11 · codec 不存在与构建能力

命令写出 `h264_ohosavcodec` 只表达使用意图。fftools 查找 codec 时，当前 `.so` 必须已经编译并注册对应平台适配。

Native 日志提示 codec 不存在时，应检查源码版本、构建选项、注册与产物，而不是反复调整同一个编码器名称。

## 自检

1. codec 名正确为何仍不可用？
2. 日志怎样把问题指向构建产物？
3. 修复需要重走哪些构建步骤？

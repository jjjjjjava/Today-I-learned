# K05 · 线程局部状态不等于并发安全

`__thread ex_buf__` 与 `thread_local g_currentCallback` 隔离跳转现场和当前任务上下文。

fftools 仍有大量全局共享状态，因此“回调不串”和“命令可并发”是两个不同结论。项目仍用单工作线程串行调度。

## 自检

1. TLS 隔离了什么？
2. 什么仍是共享的？
3. 为什么线程池大小保持 `1`？

# K12 · 主线程同步 Binder

```text
main
→ BinderProxy.transactNative
→ 某系统服务或远程接口
```

当前主线程同步等待对端返回。继续追踪：

1. 调用了哪个服务；
2. 请求是否位于响应关键路径；
3. 对端是否有慢任务、锁竞争或 Binder 线程池拥塞；
4. 是否有超时、缓存、异步或批量化边界。

“主线程 Binder 导致 ANR”只是直接现象；真正责任方可能在远端。

## 相关

- [[../阶段/06_单点归因_归并处理]]
